<script>
</script>

<main>
	<div class="grid p-5 mt-5 wood-sign">
		<div class="row justify-content-center mb-5">
			<div class="col text-center robot-title display-1">Robot</div>
		</div>
		<div class="row h-50">
			<div class="col">display robot</div>
			<div class="col rounded-content-box me-5 p-5 description-text">
				<div class="text-center h1">The base</div>
				<p>
					base explanation here with parallax effect: as the user scrolls down the page, the content
					on this “paper” (text, images, videos/clips) scroll normally
				</p>
				<p>
					while the dark panel containing the base 3d model has sticky positioning and stays in
					place until the next row is reached. see the final example (shows sticky positioning)
					here: https://developer.mozilla.org/en-US/docs/Web/CSS/position#examples. rotate
					model/camera as they scroll?while the dark panel containing the base 3d model has sticky
					positioning and stays in place until the next row is reached. see the final example (shows
					sticky positioning) here:
					https://developer.mozilla.org/en-US/docs/Web/CSS/position#examples. rotate model/camera as
					they scroll?while the dark panel containing the base 3d model has sticky positioning and
					stays in place until the next row is reached. see the final example (shows sticky
					positioning) here: https://developer.mozilla.org/en-US/docs/Web/CSS/position#examples.
					rotate model/camera as they scroll?while the dark panel containing the base 3d model has
					sticky positioning and stays in place until the next row is reached. see the final example
					(shows sticky positioning) here:
					https://developer.mozilla.org/en-US/docs/Web/CSS/position#examples. rotate model/camera as
					they scroll?while the dark panel containing the base 3d model has sticky positioning and
					stays in place until the next row is reached. see the final example (shows sticky
					positioning) here: https://developer.mozilla.org/en-US/docs/Web/CSS/position#examples.
					rotate model/camera as they scroll?while the dark panel containing the base 3d model has
					sticky positioning and stays in place until the next row is reached. see the final example
					(shows sticky positioning) here:
					https://developer.mozilla.org/en-US/docs/Web/CSS/position#examples. rotate model/camera as
					they scroll?
				</p>
			</div>
		</div>
	</div>
</main>

<style>
	.description-text {
		overflow-y: scroll;
		/* overflow: hidden; */
		height: 400px;
	}
	.robot-title {
		font-family: 'Angry Birds';
		color: white;
	}
	.wood-sign {
		background-image: url('/textures/wood-sign.png');
		background-size: 100% auto;
		background-repeat: repeat-y;
		margin-left: 10%;
		margin-right: 10%;
		/* height: 40%; */
	}
</style>
